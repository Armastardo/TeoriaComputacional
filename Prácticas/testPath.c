#include "path.c"

int main(){
	Path * paths = NULL;
	/*addStep(&paths, 0, 1);

	duplicatePath(&paths, 0);
	duplicatePath(&paths, 0);


	addStep(&paths, 1, 2);
	addStep(&paths, 2, 3);*/

	printPaths(paths, "");
/*	printf("======\n");
	printPath(paths, 1, "a");
	printf("======\n");
	printPath(paths, 2, "b");

	Vector * delete = NULL;
	addState(&delete, 0);
	addState(&delete, 1);

	printf(" == Eliminados == \n");

	deletePaths(&paths, delete);
	printPath(paths, 0, "a");*/
}